#include <stdio.h>
#include <math.h>
int main()
{
    // int width =2, height=3;
    // printf("Give a width: ");
    // scanf("%f", &width);

    // printf("Give a height: ");
    // scanf("%f", &height);
    double power = pow(20, 10);
    printf("Area is: %f\n", power);
    return 0;
}