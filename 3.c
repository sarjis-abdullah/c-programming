# include <stdio.h>
# include <math.h>
int main()
{
    // int width =2, height=3;
    // printf("Give a width: ");
    // scanf("%f", &width);

    // printf("Give a height: ");
    // scanf("%f", &height);
int power = pow(2,3);
    printf("Area is: %d\n", power);
    return 0;
}